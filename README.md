# alta_lineup_summary

Monorepo for ALTA lineup tooling.

## Structure

- `python/`: current Python CLI implementation (`alta-ratings`)
- `apps_script/`: planned Google Apps Script version (v2)

## Python version

```bash
cd python
uv run alta-ratings run
```

See `python/README.md` for full setup and `.env` instructions.
